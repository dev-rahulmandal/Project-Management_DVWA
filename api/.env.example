# VulnForge API - fake training values only.
# Copy to api/.env before running: cp api/.env.example api/.env

DATABASE_PATH=vulnforge.db
JWT_SECRET=changeme
JWT_ISSUER=vulnforge-api
WEB_ORIGIN=http://localhost:8082
API_PORT=8081
