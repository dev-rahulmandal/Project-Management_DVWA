# Prolane API - fake training values only.
# Copy to api/.env before running: cp api/.env.example api/.env

DATABASE_PATH=prolane.db
JWT_SECRET=summer2023
JWT_ISSUER=prolane-api
WEB_ORIGIN=http://localhost:8082,http://prolane.test:8082
API_PORT=8081

# Face: 0 = challenge (realistic target, default), 1 = lab (answer-key signals,
# secure /secure/* twins, OpenAPI /docs). Leave at 0 for a public deployment.
VF_LAB=0
